# 更新公告（Release Notes）

## 版本信息

| 项目 | 内容 |
|---|---|
| **项目名称** | cos-effect-prompt — COS 人像后期提示词生成 Skill |
| **版本号** | v1.0.0 |
| **发布日期** | 2026-08-26 |
| **开源协议** | MIT License |
| **代码仓库** | https://github.com/Couer869/cos-effect-prompt |

---

## 一、项目概述

本项目是一个面向 COS（cosplay）人像后期的提示词生成 Skill。它接收用户的自然语言需求，自动组装为符合目标图像模型语法的专业提示词指令，并支持以标准化预设格式导出、供图像处理插件直接导入使用。

本项目的主要目标：

- 降低 AI 后期门槛：无需记忆提示词语法，一句话生成专业指令；
- 保证处理质量：内置保底规则、透视一致性、光影一致性等硬性约束；
- 跨模型兼容：适配 Nano Banana、ComfyUI / Stable Diffusion、Midjourney、NovelAI 等主流模型；
- 持续学习：支持风格识别与学习、参考提示词库，随使用不断积累。

---

## 二、本次更新内容

### 1. 多模型适配

新增目标模型辨别机制（组装流程第 0 步）。系统依据用户明示或提示词术语自动判断目标模型，并按对应语法输出：

| 目标模型 | 输出格式 |
|---|---|
| Nano Banana（默认） | 插件预设信封 JSON（可直接导入） |
| ComfyUI / Stable Diffusion | 正向 tag + 权重 + 负向 tag + 生成参数 |
| Midjourney | 自然语言描述 + `--参数` |
| NovelAI | Danbooru 风格标签 |

各模型的保底规则、透视与光影约束均以相应语法落实。

### 2. 画笔引导（Brush Guidance）

支持用户在照片上以红色画笔标记方向引导，包括箭头（方向）、轨迹（路径/范围）、圈选（区域）。系统将红笔标记作为风、发丝、裙摆飘动及有向特效的方向基准写入提示词，并明确标注标记不残留于成片。该功能已扩展至全部目标模型格式。

### 3. 用户参考提示词库

提供用户专属参考库（`references/user-prompts.md`），支持：

- 手动或由系统自动存入参考提示词；
- 存入时执行泛化分析，提炼可复用结构与要素；
- 生成时按用途标签匹配参考条目，并按目标模型语法适配优化；
- 批量泛化分析模式，总结参考库共性。

### 4. 专业优化词学习

吸收专业后期预设的优化措辞：

- 修脸专业术语：中性灰与高低频分离、毛孔重绘（50-60%）、catchlights 眼神光、卧蚕、T 区高光等；
- 修容风格变体：自然感、厚涂感、CG 感三类；
- 3D 素材整合模块：面数提升、假法线转真实凹凸、次表面散射（SSS）、光线匹配、HSL 匹配、接触交互匹配、材质精度提升、框架锁定共 8 个子模块。

### 5. 透视一致性（硬性规则）

所有生成结果强制保持原图透视关系，包括灭点方向、相机角度与景深关系，避免新增元素产生"贴图感"。

### 6. 风格识别与学习

- 风格识别：依据线条、色彩、光影、质感、氛围五个维度，识别照片或描述的风格，输出可直接使用的风格预设；
- 风格学习：对内置库未收录的新风格，经用户确认后入库，后续生成可调用。

### 7. 参数调节控件

预设信封新增 `params` 字段，内置特效强度、风格权重、光影融入三项可调参数（含取值范围、默认值与作用位置映射），支持插件渲染为滑块控件。

---

## 三、功能模块总览

| 模块 | 功能说明 |
|---|---|
| 特效处理 | 44 类特效（魔法阵、火焰、雷电、翅膀、机械装甲、藤蔓、全息、凤凰等）+ 特效组合模板，默认 CG 质感与建模画风 |
| 修脸 | 磨皮、祛瑕疵、瘦脸、眼神光、妆容，支持自然感 / 厚涂感 / CG 感修容 |
| 头发优化 | 丝绸光泽、亮晶晶闪粉星尘、发丝细节 |
| 服装优化 | 面料焕新、褶皱管理、瑕疵修复、材质细化、C 服还原 |
| 身体塑形 | 液化瘦腰、背景变形修复 |
| 风效 | 裙摆、披风、飘带风动（支持画笔引导方向） |
| 3D 素材整合 | 置入模型真实化融合（8 个子模块） |
| 光影重塑 | 三点布光、逆光 / 侧光、色温、光比（系统化改光） |
| 反推模式 | 分析已有照片的后期处理，输出可复现指令 |
| 风格体系 | MJ 风格库、风格识别与学习 |
| 参考提示词库 | 用户参考的存储、泛化分析与生成参考 |

## 四、通用处理规则

1. **保底规则**：构图、面部、姿势、背景、全局光照、色彩伽马、透视关系保持不变；
2. **光影一致性**：所有修改区域与照片原有光源方向一致；
3. **噪点一致性**：修改区域噪点水平与原图一致；
4. **禁止全局调色**：不允许通过全局调色、压光、改色温掩盖问题；
5. **模块按需启用**：仅输出与用户需求相关的处理模块。

## 五、安装与使用

### 安装

```bash
git clone https://github.com/Couer869/cos-effect-prompt.git
```

或将 `cos-effect-prompt` 目录复制至对应 agent 的 skills 目录（Cherry Studio 为 `Data\Skills\`）。

### 使用流程

1. 向 agent 描述处理需求（纯文本即可，无需附带图片）；
2. 获取输出的提示词（JSON 预设 / tag / 自然语言，视目标模型而定）；
3. 将提示词应用于目标模型并生成结果。

### 预设导出

Nano Banana 格式输出以标准信封结构（id / title / content / category / params 等字段）提供，支持保存至下载目录并导入图像处理插件。

## 六、兼容性

- 目标模型：Nano Banana（Gemini 图像模型）、ComfyUI / Stable Diffusion、Midjourney、NovelAI；
- 运行环境：Cherry Studio、Claude Code 及其他兼容 Agent Skills 规范的环境；
- 输出格式：JSON（预设信封）、tag 文本、自然语言提示词。

## 七、后续规划

- 深化各模型支持（如 ComfyUI 的 ControlNet / LoRA 写法）；
- 扩充特效与风格词库；
- 支持批量反推与批量生成。

## 八、获取方式

| 渠道 | 地址 |
|---|---|
| GitHub 仓库 | https://github.com/Couer869/cos-effect-prompt |
| 安装命令 | `npx skills add Couer869/cos-effect-prompt` |

欢迎反馈问题与改进建议，可通过 GitHub Issues 提交。

---

*本公告由项目维护者发布，内容随版本迭代更新。*
