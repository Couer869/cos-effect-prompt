---
name: COS后期提示词
description: >
  为 COS 照片（cosplay 角色照 / 写真）生成专业级 JSON 后期指令，可直接配合 Nano Banana
  （Gemini 图像模型）使用。用户在给自己的角色照做后期时使用——加特效（魔法阵、火焰、雷电、
  冰霜、翅膀、光环、圣光、粒子、辉光、霓虹、雨、雪、花瓣、赛博街景）、做综合修图（身体塑形
  液化、服装焕新、裙摆/披风风动效果）都在范围内。本技能会输出一份结构化的 JSON 指令：
  保底规则（构图/脸/姿势不变）+ 检测分析 + 按需启用的处理模块 + 约束与光影噪点一致性。
  请务必在用户提到「给照片加特效」「COS 后期」「P 图」「修图」「加魔法阵/火焰/光环/翅膀」
  「液化/瘦身/塑形」「裙子飘起来/加风效」「衣服太皱了」等任何 COS 照片后期需求时使用本技能，
  即使他们没有说出「提示词」或「指令」——用户提到 Nano Banana / 香蕉模型 / Gemini 图像 时也务必使用。
  用户**给出照片**并问「这个效果怎么做」「分析这张图的后期」「这张图用了什么修图/特效」
  「照着这张图做」「反推一下提示词」时，进入**反推模式**：观察照片、识别已有后期处理、
  反推出可复现的 JSON 指令。
---

# COS 后期提示词生成

为一个目标服务：**把用户"想怎么处理这张 COS 照片"的模糊想法，变成一份 Nano Banana 能一次理解并执行的 JSON 后期指令**。

Nano Banana（Gemini 图像模型）吃的是自然语言指令，且它是语言模型——结构化 JSON 指令对它同样有效，甚至因为**无歧义**而更可靠：保底规则写清楚"什么不许动"，处理模块写清楚"改哪里、改成什么样"，约束兜底防止翻车。本技能的一切围绕"组装这份 JSON"展开。

## 输出：JSON 指令（核心交付）

**固定骨架**：`role`、`base_rules`、`detection`、`constraints`、`integration` 总是出现。
**按需启用**：`body_sculpting`（塑形）、`clothing_refresh`（服装）、`wind_effect`（风效）、`special_effects`（特效）——用户提了才加，没提就**不输出**该键。

完整结构见 `references/template.json`。交付时输出完整可用的 JSON，用代码块包裹方便复制，不要省略字段，也不要加注释。

## 组装流程

### 第 1 步：识别模块

读用户请求，判断启用了哪些模块：

| 用户说了… | 启用模块 |
|---|---|
| 魔法阵 / 火焰 / 雷电 / 翅膀 / 光环 / 辉光 / 粒子 / 雨 / 雪 / 花瓣 / 街景 / 加光效… | `special_effects` |
| 瘦身 / 塑形 / 液化 / 身材修一下 / 收腰… | `body_sculpting` |
| 修脸 / 磨皮 / 美肤 / 瘦脸 / 眼睛 / 妆容 / 祛痘 / 祛瑕疵… | `face_retouch` |
| 头发 / 发丝 / 亮晶晶 / 闪粉 / 光泽 / 发质… | `hair_enhancement` |
| 衣服皱 / 面料 / 瑕疵 / 污渍 / 破损 / 起球 / 开线… | `clothing_refresh` |
| 风效 / 裙摆飘起来 / 披风扬起 / 加风… | `wind_effect` |

用户没提任何模块（只说"帮我修一下这张图"）→ **先问一个关键问题**："想加特效（魔法阵/火焰/光效等）还是做综合修图（塑形/服装/风效）？"不要盲目默认。
用户提了某个**已知表里没有**的处理（调色 / 背景更换 / 光影重塑 / 去路人 / 加文字…）→ **不要拒绝、不要让人重述**，直接进入「泛化模块组装」。

### 第 2 步：填固定骨架

- `role`：固定为 `Cosplay人像后期综合处理师`
- `base_rules`：固定（构图/面部/姿势保持不变，不做缩放旋转平移）
- `detection`：固定——"分析全身身材比例、服装类型和可移动部分(裙摆/披风/飘带)、头发长度方向"
- `integration`：固定——`light_match`（所有修改区域光影一致）+ `noise_match`（噪点一致）

### 第 3 步：填按需模块

**`special_effects`（本技能核心）**：从 `references/effects.md` 查特效词库。每个特效填五个字段：

```json
"special_effects": {
  "effects": [
    {
      "type": "魔法阵",
      "location": "脚下地面",
      "visual_description": "发光的金色魔法阵，繁复几何图纹与神秘符文环绕旋转，符文线条柔和发光",
      "intensity": "明显",
      "blend": "魔法阵光芒映照角色产生金色环境光，地面法阵透视正确"
    }
  ]
}
```

- `type`：特效名（词库条目）
- `location`：位置——脚下 / 手中 / 背后 / 头顶 / 全身 / 背景，按用户描述或词库惯例
- `visual_description`：词库「视觉描述」字段，是提示词的主体，要"看得见"
- `intensity`：用户说了强度就用；没说默认 `中（标准）`。措辞参照词库「强度词」
- `blend`：词库「融入写法」字段——光影、透视、图层逻辑，决定特效"长在照片里"还是"贴上去"
- 多个特效就放 `effects` 数组，按重要性从高到低排序（主角特效在前）
- **质感风格（默认 CG）**：给所有特效附加 CG 质感/建模画风——把 `references/effects.md` 质感段的「通用质感措辞」拼进 `visual_description` 末尾、「通用融入调整」拼进 `blend`，并在 `special_effects.style` 标注风格。用户明确要求写实实拍 / 二次元手绘 / 扁平贴图等其他风格时，用对应风格覆盖默认

**`face_retouch` / `hair_enhancement` / `clothing_refresh`**：从 `references/portrait.md` 取对应子项的 JSON 值（每个子项都给了现成的中文表述，直接选用）。只输出与用户需求相关的子项，用户没提的键就省略。例如用户只说"磨皮"就只给 `skin`，不要整模块都塞进去。
- 修脸默认强度：用户没说就按"自然不失真"处理（轻-中档）
- 亮晶晶头发：默认给 `gloss` + `sparkle` 组合（光泽 + 闪粉），这是高频需求；用户说"别太闪"就去掉 `sparkle`
- 服装瑕疵：`flaw_fix` 覆盖污渍/破损/脱线/五金件，按用户具体说的取舍

**`body_sculpting` / `clothing_refresh` / `wind_effect`**：按用户需求填充，字段结构沿用参考模板（见 `references/template.json`）。塑形务必带 `background_fix`（液化导致的背景变形完全修复）。风效的 `direction` 要统一（与头发飘动方向一致）。

### 第 4 步：动态拼装 `constraints`

`constraints` 由**基础项 + 各启用模块的严禁项**拼接：

- 基础（永远有）：`严禁改变动作姿势`、`严禁液化过度变形`、`严禁背景出现变形`
- `special_effects` 启用时加：`严禁重绘人物本体（面部/服饰/体型）`、`严禁特效光源方向与照片不一致`
- `face_retouch` 启用时加：`严禁过度磨皮失真`、`严禁改变五官结构导致认不出本人`
- `hair_enhancement` 启用时加：`严禁闪粉/光点溢出到面部或背景`、`严禁改变发型轮廓`
- `clothing_refresh` 启用时加：`严禁破坏服装版型与图案`
- `wind_effect` 启用时加：`严禁风效不自然或风向不统一`
- `body_sculpting` 启用时加：`严禁液化过度、保持自然比例`
- 其他模块如有特定严禁项，从对应 reference 或参考模板取

### 第 5 步：校验与输出

- 检查 JSON 键名拼写与参考模板完全一致
- 每个启用的模块都有实质内容，不要输出空对象
- 输出一份完整 JSON，代码块包裹

## 泛化模块组装（库中没有的需求）

知识库覆盖不了所有后期需求（调色、背景更换、光影重塑、去路人、加文字……）。**遇到库中没有的处理，不要拒绝、不要让人重述**——用下面的模板现造一个模块，质量与已知模块同一标准。

### 第 1 步：从用户原话提取四要素

| 要素 | 键 | 说明 |
|---|---|---|
| 动作 | `action` | 用户要做什么：替换 / 调整 / 添加 / 修复 / 重塑 / 强化 / 消除…，用强动词 |
| 对象 | `target` | 作用对象：整体色调 / 背景 / 光源 / 画面中某物体… |
| 目标描述 | `goal_description` | 期望的视觉结果，写成"看得见"的画面描述——具体到颜色、质感、光线、氛围，参考 effects.md 的视觉描述写法 |
| 保持项 | `preserve` | 必须保持不变的部分（默认：人物本体、动作、构图） |

强度适用就加 `intensity`，措辞参照效果库的强度表。专业表达从 `references/nano-banana.md` 的摄影/电影术语里取（调色→色温/色调/LUT、光影→体积光/轮廓光/阴影）。

### 第 2 步：按泛化模板组装

```json
"<module_name>": {
  "action": "调整",
  "target": "整体色调",
  "goal_description": "冷青蓝电影调色，暗部沉稳、高光带冷色，肤色与背景同色系协调",
  "preserve": "人物本体、面部细节、动作与构图",
  "intensity": "中",
  "blend": "与照片原有光源方向一致，避免局部变色"
}
```

- `<module_name>` 用语义化小写命名：调色→`color_grading`、背景更换→`background_replace`、光影重塑→`lighting_reshape`、去路人→`remove_object`；没现成英文就按语义自造
- 新模块放在固定骨架之后、`constraints` 之前，与已知模块并列
- 为该模块在 `constraints` 里追加一条针对性严禁项（如背景更换→"严禁人物边缘白边/生硬抠图痕迹"）
- 保底规则、`integration`、CG 质感风格逻辑与已知模块完全一致

### 参考示例
调色 / 背景更换 / 光影重塑 / 去路人的完整组装见 `references/fallback.md`。

## 反推模式（照片 → 提示词）

用户**提供照片**并问"这个效果怎么做 / 分析这张图的后期 / 照着这张图做 / 反推一下提示词"时，进入此模式。目标：从照片里**识别出已有的后期处理**，反推出能复现它的 JSON 指令。这是主流程的逆向——输入是一张已处理过的照片，输出是"再造它"的指令。

### 第 1 步：仔细观察照片

如果模型有视觉能力，仔细观察照片，逐项检查：

- **特效痕迹** → 对照 `references/effects.md`：魔法阵 / 火焰 / 雷电 / 粒子 / 光环 / 翅膀…？位置在哪？强度轻中强？CG 质感还是写实质感？
- **修脸 / 塑形痕迹** → 对照 `references/portrait.md`：皮肤磨皮程度（自然保留质感 or 光滑塑料感）、脸型 / 身材是否有液化痕迹、头发是否有光泽或闪粉
- **色调与光影** → 判断风格：色温冷暖、调色倾向（电影感 / 赛博 / 梦幻 / 黑白）、光源方向、是否逆光 / 轮廓光 / 体积光、整体是 CG 建模质感还是实拍写实
- **场景与氛围** → 背景类型（夜景 / 废墟 / 古堡 / 星空…）、天气氛围、整体情绪

**看不到照片时**（模型无视觉能力），向用户询问关键特征，不要瞎猜：照片里有什么特效 / 皮肤和脸的状态 / 色调风格 / 场景背景。

### 第 2 步：映射到模块

把识别出的处理点映射到对应模块，**只启用照片里真实存在的处理**（没看到就不输出）。识别出但知识库没有的处理 → 用泛化模块组装。

### 第 3 步：反推复现指令

输出 = **简短分析 + 复现 JSON 指令**。JSON 仍按主流程组装，`visual_description` / `blend` 写"照片里看到的效果长什么样"。

```markdown
## 反推分析
- 特效：脚下金色发光魔法阵（中强，CG 质感，符文旋转）
- 修图：轻微磨皮保留质感，冷色调电影调色
- 场景：夜景街景，霓虹氛围

## 复现指令
```json
{
  "role": "Cosplay人像后期综合处理师",
  "base_rules": { "preserve_composition": true, "preserve_face": true, "preserve_pose": "不改变动作姿势", "no_scale_rotate_translate": true },
  "special_effects": {
    "style": "CG质感，建模画风……",
    "effects": [
      { "type": "魔法阵", "location": "脚下地面", "visual_description": "……照片里看到的效果……", "intensity": "中强", "blend": "……" }
    ]
  },
  "constraints": { "严禁": "……", "禁止": "……" },
  "integration": { "light_match": "所有修改区域光影一致", "noise_match": "噪点一致" }
}
```
```

### 第 4 步：结合用户要求

如果用户在"照这张图"之外还有新要求（如"把特效改成蓝色" / "不要这个魔法阵换成火焰"），在反推基础上调整对应字段再输出。这时输出的是**融合指令**：保留照片里想复现的部分，改掉要改的部分。

### 反推注意事项

- 只描述照片里**看得到**的处理，不臆造不存在的效果
- 强度看不清就用"中"兜底
- 分析部分简短（3-5 条），可复现指令完整（按主流程）
- 反推模式同样遵守"按需启用"——照片里没有的模块一律不输出

## 追问规则（克制）

- 没提任何模块 → 问一次方向（特效 or 综合修图）
- 提了特效没说类型 → 从 `references/effects.md` 举例让用户挑（最多一次）
- 说了类型没说强度/位置 → 用默认值，不要连环追问

## 引用知识库

- 特效怎么写 → `references/effects.md`（四大类 28 种特效，含视觉描述 / 融入写法 / 强度词 / 变体）
- 修脸 / 亮晶晶头发 / 服装瑕疵 → `references/portrait.md`（含现成 JSON 值、严格项、组合示例）
- 库中没有的需求 → `references/fallback.md`（泛化模块：调色/背景更换/光影重塑/去路人）
- 完整 JSON 骨架 → `references/template.json`
- Nano Banana 编辑原理与摄影术语 → `references/nano-banana.md`
